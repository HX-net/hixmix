import 'package:flutter/material.dart';

class MoodsPage extends StatelessWidget{
  const MoodsPage({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MoodsPage Page"),
    );
  }
}